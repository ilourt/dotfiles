#/bin/bash


SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" &> /dev/null && pwd)
REPO_DIR="$(dirname "$SCRIPT_DIR")"
echo $REPO_DIR/nvim

# Install nvim config
ln -s "$REPO_DIR/nvim" $HOME/.config/nvim

# Install wezterm config
ln -s "$REPO_DIR/wezterm" $HOME/.config/wezterm
